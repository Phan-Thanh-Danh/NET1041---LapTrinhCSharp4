@model Bai1.Models.Student

@{
    ViewData["Title"] = "Xóa sinh viên";
}

<div class="row justify-content-center animate-fade-in" style="margin-top: 50px;">
    <div class="col-md-6">
        <div class="card-custom text-center">
            <h3 class="text-danger mb-3">⚠️ Xác nhận xóa</h3>
            <p class="mb-4">Bạn có chắc chắn muốn xóa sinh viên này không?</p>

            <div class="alert alert-light border mb-4">
                <h4 class="mb-1">@Model.Name</h4>
                <p class="text-secondary mb-0">@Model.Email</p>
            </div>

            <form asp-action="Delete" method="post" class="d-flex justify-content-center gap-3">
                <input type="hidden" asp-for="Id" />
                
                <a asp-action="Index" class="btn btn-secondary">Hủy bỏ</a>
                <button type="submit" class="btn btn-danger">🗑 Xóa ngay</button>
            </form>
        </div>
    </div>
</div>
